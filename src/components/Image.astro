---
interface Props
{
  image: string ;
  name: string ;
  width: number ;
}

const { image, name, width } = Astro.props ;
---

<img 
  src={ `/images/${ image }.webp` }
  alt={ name }
  title={ name }
  loading="eager" 
  draggable="false"
  style={ `width: ${ width }px ;` }
  class="skillsImg"
/>